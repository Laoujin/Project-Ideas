<p>Having recently read “Test-Driven Development: By Example”, I started up my editor to give it a try developing Monopoly - I heard “Design me Monopoly” is an often asked interview question. Usually I write a crude version of a whole user story then run the application to test it. Lots of times obviously - fixing bugs,&nbsp; adding features and refactoring between each run. Using the compiler errors to find the next place to write more code and fixing all regressions – a few times each.</p> <p>So no more headaches trying to wrap my head around everything, let’s have some tests to have a much quicker feedback cycle and a more constrained environment for me so I don’t start adding client/server code before any game is even played. (But of course you want to play a game with someone over the internet…)</p> <p>For my first test, I ended up with a Game to which you can add Players. I first named the method AddPlayer but while writing this I realized you don’t AddPlayer, a Player Joins the game. Once everyone has joined, we tell the game to Play – which doesn’t sound quite right. But at the moment it still looks like a good idea to have the game keep track of who’s turn it is etc. The first two assertions are that the Move returned by Game.Play was done by the first Player and that his position is the same as the first dice Thown. </p> <p>[TestMethod]<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; public void PlayersThrowAndMoveOnTheBoard()<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; {<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; Game game = new Game();<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; Player wouter = new Player("Wouter");<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; Player sandra = new Player("Sandra");<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; game.AddPlayer(wouter);<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; game.AddPlayer(sandra);  <p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; Move wouterMove = game.Play();  <p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; Assert.AreSame(wouterMove.Player, wouter);<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; Assert.AreEqual(wouterMove.Thrown, game.GetPosition(wouter));<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; } <p>This is actually pretty neat, while the Move and the GetPosition both return a hardcoded 5, which looks pretty silly, I have proven something about the code with an implementation I only need to improve when there is need for… by another test? Where to go from here. I was first tempted to make the current code work for multiple turns, with random dice throws and remembering positions on the board but all these are technically trivial to implement while that Move object is sitting there mysteriously. How can the CurrentPlayer interact with his Move to buy some Street. A passive Move seems to not only be the destination of a Player, it looks like another object is needed that allows the Player to interact with the Game. <p>So it looks like I already coded too much.  <p>It seemed as if a player doing the Play was weird but it makes perfectly sense. A player decides when to play with Throw. If it’s not your turn you get an awful null. <p>Keeping track of two things: Who’s playing and the location of the players on the board. It started dividing my mind over two distinct problems, so I delete the GetLocation part of the (test)code and continue to try to buy something. <p>The new test becomes: <p>The tests I’m writing are already pretty much integration tests with multiple assertions each. The test is failing but why? I need to debug to find out which ones are not yet working. So I’m splitting up the tests more, taking smaller steps and rely on the tests instead of manually debugging by stepping through the tests. 
